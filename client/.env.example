# Where the browser opens the Socket.IO connection.
# Leave empty in development (Vite proxies to the API). In production set it to the API origin,
# for example https://codemate-api.onrender.com
VITE_SOCKET_URL=

# Development only: where the Vite proxy forwards /api and /socket.io.
DEV_API_TARGET=http://localhost:5000
